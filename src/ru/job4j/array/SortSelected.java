package ru.job4j.array;

public class SortSelected {

    public static int[] sort(int[] data) {
        for (...) {
            int min = MinDiapason.findMin(data, 0, data.length);
            int index = FindLoop3.indexOf(data, min, 0, data.length);
            /* swap(...) */
        }
        return data;
    }
}
